# Printables — home-intercom enclosure

Metadata and files for publishing the 3D model (e.g. Printables, MakerWorld).

---

## Model name *(required)*

**home-intercom — enclosure (RPi Zero 2 W + ReSpeaker 1.1)**

---

## Summary *(required)*

Enclosure for Raspberry Pi Zero 2 W with clearance for the ReSpeaker 2-Mic HAT v1.1, push button, and recording LED. Wall / doorframe intercom mount.

---

## Main category *(required)*

**Electronics → Raspberry Pi / GPIO**

*(adjust for the platform: e.g. “Gadgets”, “Household”, “Organization”)*

---

## Additional tags

`raspberry-pi` `rpi-zero` `respeaker` `intercom` `doorbell` `enclosure` `wall-mount` `seeed`

---

## Description

### Purpose

Enclosure for the [home-intercom](https://github.com/kopringo/home-intercom) project: an intercom on Raspberry Pi Zero 2 W with a ReSpeaker microphone and GPIO button support.

### Hardware compatibility

| Part | Version / notes |
|------|-----------------|
| Board | Raspberry Pi Zero 2 W |
| Audio HAT | Seeed ReSpeaker 2-Mic Pi HAT v1.1 |
| Button | GPIO 12 (pull-up) |
| Recording LED | GPIO 13 |

### Printing

- **Material:** PLA or PETG (indoor / outdoor in sun — PETG)
- **Orientation:** *[fill in — e.g. flat on bed, lid facing up]*
- **Infill / walls:** *[fill in, e.g. 20%, 3 perimeters]*
- **Supports:** *[yes / no — where]*

### Assembly

1. *[step by step — e.g. M2.5 screws, adhesive, standoffs]*
2. *[access to SD, USB, button]*

### Notes

- The model includes the enclosure only — no electronics.
- Verify dimensions for your HAT and button before printing.
- Assembly photos: [`images/`](images/) directory below.

### 3D model license

*[e.g. CC BY 4.0 / CC0 — match the files in `models/`]*

---

## Images

Place renders, assembly shots, and detail photos in:

```
printables/images/
```

| File | Description |
|------|-------------|
| *(e.g. `01-exploded.png`)* | Exploded view |
| *(e.g. `02-assembled-front.jpg`)* | Assembled — front |
| *(e.g. `03-mounted-wall.jpg`)* | Wall-mounted install |
| *(e.g. `04-internal-clearance.jpg`)* | Interior / HAT clearance |

> Directory: [`images/`](images/) — add files and update the table.

---

## Model files

Printable geometry (STL, 3MF, STEP, etc.) in:

```
printables/models/
```

| File | Description |
|------|-------------|
| *(e.g. `enclosure-bottom.stl`)* | Bottom shell |
| *(e.g. `enclosure-top.stl`)* | Lid / top cover |
| *(e.g. `button-cap.stl`)* | Button cap |
| *(e.g. `full-assembly.3mf`)* | Multi-part project file (optional) |

> Directory: [`models/`](models/) — add files and update the table.
